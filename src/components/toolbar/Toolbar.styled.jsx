import styled from 'styled-components';

export const Wrapper = styled.div(() => ({
  display: 'flex',
  position: 'fixed',
  top: 0,
  left: 0,
}));

export const BoxesCount = styled.span(() => ({
  color: 'white',
}));
